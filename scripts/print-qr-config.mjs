/**
 * URLs used for high-res print QR PNGs (4096×4096).
 * Keep googlePlaceId in sync with src/components/Footer.tsx (GOOGLE_PLACE_ID).
 *
 * menuSiteUrl: HTTPS origin where your menu app is deployed (same as “Scan for Menu”
 * in the footer — no trailing slash). Change this if your live URL differs.
 */
export const googlePlaceId = 'ChIJIQ1w0qGHSxYRPo9ePbStR_M'

export const menuSiteUrl = 'https://messenta-coffee-menu.vercel.app'
