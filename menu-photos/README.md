# Drop new menu photos here

1. Copy shoot JPGs into this folder (keep original names).
2. Run: `node scripts/process-menu-photos.mjs`
3. Seed / update products via `supabase/seed/` and `/admin`.

Everything except this README is **gitignored** so large originals stay off GitHub.
Processed images for the app live in `public/menu/`.
